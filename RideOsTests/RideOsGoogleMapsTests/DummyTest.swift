import RideOsGoogleMaps
import XCTest

// Since we don't have any tests for RideOsGoogleMaps ATM, just add a dummy test so that this target builds. This
// allows us to at least validate that RideOsGoogleMaps builds as part of CI
class DummyTest: XCTestCase {
    func testStuff() {
        XCTAssertTrue(true)
    }
}
